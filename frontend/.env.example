# 前端环境配置
# 方案一：动态获取当前访问的域名（推荐，适用于任何内网环境）
# 设置为相对路径，前端运行时会自动拼接当前访问的域名
VITE_API_BASE_URL=/api/v1

# 方案二：固定内网 IP（不推荐，网络环境变化时需要修改）
# VITE_API_BASE_URL=http://192.168.42.208:8011/api/v1

# 方案三：本地开发（仅本地使用）
# VITE_API_BASE_URL=http://localhost:8011/api/v1

# 说明：
# - 使用方案一时，前端请求会通过 Vite Proxy 转发到后端
# - vite.config.ts 中配置了 proxy: '/api' -> 'http://localhost:8011'
# - 内网其他电脑访问时，浏览器会自动使用当前访问的域名（如 http://192.168.x.x:5173）
# - 这样不需要修改配置，任何内网环境都可以访问
